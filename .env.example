# AutoForms Configuration Example
# Copy this file to .env and fill in your actual values

# OpenAI Configuration
OPENAI_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Database Configuration
MONGODB_URI=mongodb://localhost:27017
DATABASE_NAME=autoforms

# JWT Security (IMPORTANT: Use a strong, unique secret in production)
JWT_SECRET=your_very_secure_jwt_secret_key_here_at_least_32_characters_long

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
EMAIL_FROM=your_email@gmail.com

# Application Configuration
BASE_URL=http://127.0.0.1:8083
ADMIN_EMAILS=admin@example.com,another-admin@example.com

# Security Note:
# - Never commit this file with real credentials
# - JWT_SECRET should be a random string of at least 32 characters
# - For Gmail SMTP, use an app-specific password, not your regular password
# - ADMIN_EMAILS should be comma-separated list of admin email addresses