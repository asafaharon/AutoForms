# Core FastAPI and ASGI server
fastapi==0.95.2
uvicorn[standard]==0.22.0

# Template engine
jinja2==3.1.2

# Database drivers
motor>=3.5.1  # MongoDB async driver
pymongo>=4.6.0  # MongoDB sync driver (for compatibility)

# HTTP client for external APIs
httpx==0.27.0

# Email functionality
aiosmtplib==2.0.2

# PDF generation
weasyprint>=65.0

# Language detection for international content
langdetect==1.0.9

# AI/ML integrations
openai>=1.0.0

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.1.2

# File uploads and form handling
python-multipart==0.0.6

# Caching removed - Redis dependency eliminated for simpler deployment

# Production performance optimizations (Linux/Mac only)
uvloop>=0.17.0; sys_platform != "win32"  # High performance event loop
httptools>=0.6.0; sys_platform != "win32"  # Fast HTTP parser

# Production monitoring and logging
prometheus-client>=0.16.0  # Metrics collection
structlog>=23.0.0  # Structured logging

# Environment and configuration
python-dotenv>=1.0.0  # .env file support

# Production WSGI server (alternative to uvicorn)
gunicorn>=21.0.0; sys_platform != "win32"

# Security enhancements
cryptography>=41.0.0  # Latest crypto library
pycryptodome>=3.18.0  # Additional crypto support

